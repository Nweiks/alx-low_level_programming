#include <stdio.h>
#include <stdlib.h>

/**
 *main - entry point
*Return: 0
 */

int main(void)
{
	int c, n;
	printf("Your password is\n");

	for (c = 1; c <= 1; c++)
	{
		n = rand()%9999 + 1000;
		printf("%d\n", n);
	}
	return (0);
}
