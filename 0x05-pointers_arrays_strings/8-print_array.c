#include "main.h"

/**
*print_array - prints an array
*@a: int
*@n: int
*Return: Nothing
*/

void print_array(int *a, int n)
{
int i:

for (i = 0; i < n; i++)
{
printf("%d," a[i]);
else 
printf("%d," a[i]);
}
printf("\n");
}
